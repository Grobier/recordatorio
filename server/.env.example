# Base de datos SQLite (ruta relativa a /server)
DATABASE_URL="file:./prisma/dev.db"

# API
PORT=4000
CLIENT_ORIGIN=http://localhost:3000
# URL base usada en los enlaces de confirmacion/cancelacion
BASE_URL=http://localhost:4000

# Config SMTP (ejemplo con Gmail en modo app password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_TIMEOUT_MS=15000
SMTP_USER=grobier.2h@gmail.com
SMTP_PASS=odbvazmghwxrgqcw
MAIL_FROM="Kinesiologo Lorenzo Grobier <grobier.2h@gmail.com>"

# Nombre por defecto del profesional (opcional)
DEFAULT_PROFESIONAL="Lorenzo Grobier"